//for sorting

function sortArray(arr , n){
  arr.sort((a,b)=>a-b);
}

//storing value in a set

newSet = new Set();

//sorting characters of a string
steps 1. turn STRING to ARRAY
      2. sort the ARRAY
      3. turn back ARRAY to STRING
code: const sorted_t = t.split('').sort().join(''); 


//hashmap and collection use of java

//new hash map declaration in javascript
let newMap = new Map();

// two new funtions learned in javascript [4/4/21]
array.indexOf(element)  //it will give the index number of the outpt

Math.max(...array) //it will return the max in the array

//finding peak element one liner
arr.indexOf(Math.max(...arr))
